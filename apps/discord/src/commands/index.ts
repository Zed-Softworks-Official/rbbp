import { registerRole } from './register-role'
import { unregisterRole } from './unregister-role'
import { registerLogChannel } from './log-channel'

export const commands = {
    registerRole,
    unregisterRole,
    registerLogChannel,
}
